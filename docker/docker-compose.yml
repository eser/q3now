# ══════════════════════════════════════════════════════════════════════════════
# q3now dedicated server — Docker Compose example
# ══════════════════════════════════════════════════════════════════════════════
#
# Quick start:
#   1. Place your pak0.pk3 in ./server-data/baseq3/
#   2. (Optional) Place a custom server.cfg in ./server-data/baseq3/
#   3. docker compose up -d
#
# The server will be reachable at <host-ip>:27960 (UDP).
# ══════════════════════════════════════════════════════════════════════════════

services:
  q3now-server:
    image: eserozvataf/q3now:latest
    # To build locally instead of pulling:
    # build:
    #   context: ..
    #   dockerfile: Dockerfile

    ports:
      - "27960:27960/udp"

    environment:
      WIRED_HOSTNAME: "q3now Docker Server"
      WIRED_MAXCLIENTS: "16"
      WIRED_RCONPASSWORD: "changeme"
      WIRED_SCORELIMIT: "25"
      WIRED_TIMELIMIT: "15"
      # WIRED_GAMETYPE: "0"          # 0=FFA, 1=Duel, 3=TDM, 4=CTF
      # WIRED_MAPROTATION: "q3dm17 q3dm6 q3tourney2"
      # WIRED_QUIC: "1"              # Enable QUIC transport (needs certs)
      # WIRED_DOWNLOAD_URL: "http://yoursite.com/q3now"
      # WIRED_EXEC: "server.cfg"     # Execute a custom config on startup
      # WIRED_HUNKMEGS: "128"        # Increase for many custom maps

    volumes:
      # Game data: place pak0.pk3, custom maps (.pk3), and server.cfg here
      - ./server-data/baseq3:/home/wired/baseq3
      # QUIC TLS certificates (uncomment if using QUIC)
      # - ./certs:/home/wired/certs:ro

    # Override the default map
    command: ["+map", "q3dm17"]

    restart: unless-stopped

    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "1.0"
