package engine

// Platform-specific launch behavior for macOS.
// Currently no macOS-specific logic needed — launch.go handles everything.
// This file exists to satisfy the build constraint structure if needed later
// (e.g., for macOS-specific entitlement handling or Dock integration).
