# Wired/q3now test inventory

This directory contains two deliberately separate evidence layers:

- **Host subsystem contracts** are native, asset-free executables orchestrated by CTest.
- **Process and visual gates** boot a product binary and own their explicit fixture/content setup.

`make test-host` is the canonical host command. It fails when the configured tree contains no `host-only` tests; `make check` and therefore the release target consume the same command.

## Host subsystem matrix

| CTest name | Condition | Owner | Production seam | Failure class | Labels |
|---|---|---|---|---|---|
| `wired_curve` | always | Wired math | production-config `wired_curve.c` spline math | contract | `host-only`, `subsystem`, `owner.wired-math`, `failure.contract`, `feature.always` |
| `wired_scene_eval` | always | Wired scene | scene sampling/playback evaluator | contract | `host-only`, `subsystem`, `owner.wired-scene`, `failure.contract`, `feature.always` |
| `sg_registry` | always | q3now save | callback registry | contract | `host-only`, `subsystem`, `owner.q3now-save`, `failure.contract`, `feature.always` |
| `sg_savefile` | always | q3now save | save header/RLE codec | contract | `host-only`, `subsystem`, `owner.q3now-save`, `failure.contract`, `feature.always` |
| `sg_serialize` | always | q3now save | field serializer/relocation | contract | `host-only`, `subsystem`, `owner.q3now-save`, `failure.contract`, `feature.always` |
| `sg_psw` | always | q3now save | persistent subset codec | contract | `host-only`, `subsystem`, `owner.q3now-save`, `failure.contract`, `feature.always` |
| `sg_reloc` | always | q3now save | two-phase pointer fixup | contract | `host-only`, `subsystem`, `owner.q3now-save`, `failure.contract`, `feature.always` |
| `mdl_anim` | always | Wired renderer model | Q1 MDL animation derivation | contract | `host-only`, `subsystem`, `owner.wired-renderer-model`, `failure.contract`, `feature.always` |
| `server_info_parse_contract` | always | q3now client | strict browser infoResponse parsing and bounds | security contract | `host-only`, `subsystem`, `owner.q3now-client`, `failure.security-contract`, `feature.always` |
| `cl_info_challenge_contract` | always | q3now client | generation-mixed lowercase browser authority-token derivation under repeated RNG bytes | security contract | `host-only`, `subsystem`, `owner.q3now-client`, `failure.security-contract`, `feature.always` |
| `cl_ping_queue_contract` | always | q3now client | directed-ping terminal retirement and deterministic saturated-queue oldest selection | contract | `host-only`, `subsystem`, `owner.q3now-client`, `failure.contract`, `feature.always` |
| `cl_ping_owner_contract` | always | q3now client | direct/local/global/favorites ping transaction ownership and exact browser-source identity mapping | contract | `host-only`, `subsystem`, `owner.q3now-client`, `failure.contract`, `feature.always` |
| `cl_demo_frame_contract` | always | q3now client | exact current-protocol frame classification/output atomicity plus pinned production-Huffman encode/decode of bounded illegal-command and snapshot-areamask fixtures | security contract | `host-only`, `subsystem`, `owner.q3now-client`, `failure.security-contract`, `feature.always` |
| `bot_slot_identity_contract` | always | q3now server | monotonic bot allocation identity, strict command token parsing and same-slot ABA rejection | security contract | `host-only`, `subsystem`, `owner.q3now-server`, `failure.security-contract`, `feature.always` |
| `vm_interpret_policy_contract` | always | Wired VM | production VM candidate policy: bytecode `.wasm` only, compiled `.aot` then `.wasm`, native DLL then compiled fallback | contract | `host-only`, `subsystem`, `owner.wired-vm`, `failure.contract`, `feature.always` |
| `cvar_value_contract` | always | Wired cvars | production typed bool/int/float/enum lexical and range validator | security contract | `host-only`, `subsystem`, `owner.wired-cvars`, `failure.security-contract`, `feature.always` |
| `fs_qpath_contract` | always | Wired VFS | production lexical qpath parent-component and legacy `::` rejection before host I/O | security contract | `host-only`, `subsystem`, `owner.wired-vfs`, `failure.security-contract`, `feature.always` |
| `release_provenance_contract` | always | q3now release | production Makefile recursive bundle tuple, exact artifact postcondition, pack stamp, and macOS runtime-artifact staging exclusion across Darwin/Linux/MSYS2 recipe branches | package provenance | `host-only`, `infrastructure`, `owner.build`, `failure.package-provenance` |
| `ral_feature_policy_contract` | always | Wired RAL | mandatory renderer-layer policy; retired optional feature token absent from code/build/migration-tool surfaces | contract | `host-only`, `infrastructure`, `owner.ral`, `failure.contract` |
| `ssao_feature_policy_contract` | always | Wired RAL/GTAO | current default ON plus explicit ON/OFF compile policy; override-safe header and the configured renderer compile command must agree on exact `FEAT_SSAO=1/0` | contract | `host-only`, `infrastructure`, `owner.ral`, `failure.contract`, `feature.ssao` |
| `ral_retired_feature_rejected` | always | Wired RAL | stale optional RAL cache argument is rejected at configure time rather than silently accepted | configuration | `host-only`, `infrastructure`, `owner.ral`, `failure.configuration` |
| `ral_public_surface_contract` | always | Wired RAL | callerless raw plural-bind/renderpass/framebuffer cohort absent, renderer command recording has no `qvkCmdBeginRenderPass`/`qvkCmdEndRenderPass`/`qvkCmdNextSubpass`, SMAA has no raw pipeline/render-pass/framebuffer sibling cohort, native adoption/getters isolated to the Vulkan migration bridge, graphics pipelines dynamic-rendering-only | contract | `host-only`, `infrastructure`, `owner.ral`, `failure.contract` |
| `ral_raw_api_boundary_source_policy_contract` / `ral_raw_api_boundary_reintroduction_rejected` | always | Wired renderer/RAL portability | renderer ABI 21 carries Vulkan instance/surface interop as opaque/fixed-width identities, SDL alone performs native conversion, portable/common/game sources reject Vulkan/Metal/WebGPU/OpenGL SDK types and includes, and the existing `renderervk` raw-Vulkan migration cohort is capped monotonically so TASK-202.5 can only reduce debt | contract | `host-only`, `infrastructure`, `owner.ral`, `failure.contract`, `backend.webgpu` |
| `ral_public_surface_reintroduction_rejected` | always | Wired RAL | mutations reintroducing a retired symbol or leaking a native bridge declaration into portable RAL headers fail the exact surface contract | contract | `host-only`, `infrastructure`, `owner.ral`, `failure.contract` |
| `ral_host_import_contract` | always | Wired RAL | backend-neutral loader/surface host contract, exact userdata/platform forwarding and output-atomic rejection | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_host_symbol_policy_contract` | always | Wired RAL | archive-level rejection of renderer/qcommon `ri`/`Q_str*`/`Com_sprintf` imports | link policy | `host-only`, `infrastructure`, `owner.ral`, `failure.link-policy` |
| `ral_vulkan_translate_contract` / `ral_transition_command_contract` / `ral_transition_source_policy_contract` | always | Wired RAL portability | exact production RAL→Vulkan mappings plus backend-neutral buffer/texture usage states, bounded ranges and logical queues; whole-resource shared-queue batches validate before one explicit Vulkan barrier, while distinct Vulkan families use an exact generation-bound release/acquire receipt and WebGPU can validate the same logical handoff at encoder/pass boundaries without native queue-family concepts. Duplicate, stale, canceled, partial and same-family misuse fail closed; untyped legacy maps/native barriers cannot forge portable state. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_command_lifecycle_contract` / `ral_webgpu_command_lifecycle_contract` / `ral_submission_lifecycle_contract` / `ral_command_lifecycle_source_policy_contract` | always | Wired RAL portability | backend-neutral command receipts bind opaque backend/command identities, logical graphics/compute/transfer queues and monotonic recording generations. Begin→End→Submit publishes atomically only after matching backend success; stale, duplicate, cross-backend, wrong-queue, saturated and partial batches fail closed, device loss remains unsubmitted, and a dedicated WebGPU-shaped fixture models createCommandEncoder→finish→queue.submit/cancel without native handles. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_capability_profile_contract` / `ral_webgpu_capability_profile_contract` / `ral_capability_profile_source_policy_contract` | always | Wired RAL portability | a versioned native-free 18-entry profile classifies required and optional rendering, binding, synchronization, presentation, compression and limit capabilities as native, emulated or disabled. Required gaps, malformed booleans, ordering drift and under-limit devices reject output-atomically; the Vulkan caps adapter probes sampled BC/ASTC/ETC2 families, the WebGPU-shaped fixture pins uniform-backed inline bytes/bounded bindings/shared queues, and the native Metal core publishes the same profile from a real `MTLDevice`. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_allocation_receipt_contract` / `ral_webgpu_allocation_receipt_contract` / `ral_allocation_receipt_source_policy_contract` | always | Wired RAL portability | generation-bound allocation receipts expose logical device/upload/readback/transient class, committed size/alignment, residency, native/emulated placement and allocation-time pressure without native heap/type handles. Vulkan-owned buffers/textures publish exact dedicated or suballocated placement while adopted wrappers remain non-owning; WebGPU-shaped managed placement is explicitly emulated. Invalid alignment/overflow, incompatible mapping/lazy facts, over-budget placement, saturation and malformed booleans reject output-atomically. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_suballocation_contract` / `ral_vulkan_suballocation_contract` / `ral_suballocation_source_policy_contract` | always | Wired RAL portability | a native-free bounded aligned first-fit ledger publishes exact generation-bound block/slice receipts, deterministically reuses holes and coalesces out-of-order frees. A dispatch-injected Vulkan fixture proves physical block reuse, separate buffer/image pools, shared host mapping and final-child release. Production Vulkan places small ordinary buffers and optimal images into reusable per-memory-type blocks, binds at exact slice offsets, and releases physical memory only after the final deferred child; large, transient and lazy resources remain dedicated. WebGPU explicitly retains managed placement rather than exposing native heaps. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_transient_plan_contract` / `ral_webgpu_transient_plan_contract` / `ral_transient_source_policy_contract` | always | Wired RAL portability | a fixed-capacity native-free interval planner assigns canonical transient requests to deterministic lowest compatible slots, measures peak bytes/alias savings and rejects overlap, incompatibility, overflow, capacity and OOM output-atomically. One borrowed immutable plan advances through planned→submitted→fence-retired or pre-submit canceled authority. Vulkan-capable policy may alias non-overlapping resources; WebGPU-shaped policy stays managed/disjoint without inventing native alias support. Physical Vulkan consumption is covered by the adjacent transient texture cohort contract. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_frame_graph_contract` / `ral_frame_graph_execution_contract` / `ral_frame_graph_transient_contract` / `ral_frame_graph_record_contract` / `ral_frame_graph_submit_contract` / `ral_frame_graph_source_policy_contract` | always | Wired above-RAL frame graph | a bounded native-free graph description compiles logical resources, pass-local read/write states and explicit dependencies into merged RAW/WAR/WAW/state/queue edges, stable topological order, transition facts, logical cross-queue handoffs and exact first/last-pass transient requests. A generation-bound compiler joins every logical resource to a whole physical RAL buffer/texture range, expands same-queue transitions versus paired dedicated-queue ownership operations, and produces deterministic physical timeline batches. The transient materializer exact-joins the graph plan to a physical RAL texture cohort, projects portable execution bindings, and measures disjoint-equivalent versus distinct physical committed bytes from authoritative allocation receipts; Vulkan aliasing reports positive saving while WebGPU-shaped managed/disjoint allocation reports none. The recorder preflights exact RECORDING commands/pass callbacks, then uses only public RAL commands for acquire → transition → pass → release. The submit transaction validates live timeline bases, ends every command, submits the exact wait/signal cohort once per physical batch and publishes either a complete receipt or an honest submitted-prefix failure receipt while cancelling the remaining commands. Timeline-free WebGPU-shaped shared queues use one submission without invented semaphores. Duplicate/stale/cyclic/malformed inputs reject output-atomically. Product pass callers remain a later leaf. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `frame-graph`, `feature.always` |
| `ral_transient_texture_cohort_contract` / `ral_transient_texture_source_policy_contract` | always | Wired RAL portability | a native-free physical texture-cohort owner creates all candidates before publication, allocates exactly one slot backing per deterministic transient plan, binds every logical texture, rejects borrowed/cross-role aliases output-atomically, and releases child textures before slot allocations only after a generation-bound canceled or fence-retired batch receipt. Vulkan supplies the physical alias adapter; WebGPU remains on managed/disjoint planning. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_transfer_lifecycle_contract` / `ral_webgpu_transfer_lifecycle_contract` / `ral_transfer_source_policy_contract` | always | Wired RAL portability | a native-free upload/readback receipt binds exact resource identity+generation, byte/subresource range, queue, budget and monotonic transfer/submission/completion generations. Synchronous, native-async and WebGPU-managed async outcomes advance prepared→submitted→completed or pre-submit canceled output-atomically; false fence completion, stale authority, overflow and malformed buffer/texture ranges reject. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_readback_owner_contract` / `ral_webgpu_readback_owner_contract` / `ral_readback_source_policy_contract` | always | Wired RAL portability | a generation-bound readback owner exact-joins source transfer authority, bounded READBACK staging allocation, submission/fence identity and typed map ticket. Vulkan-shaped immediate READY and WebGPU-shaped PENDING→READY/cancel maps share one lifecycle; map-before-completion, stale tickets, early/duplicate release, create/submit/alias failures and wrong receipts reject output-atomically. Vulkan buffer/texture adapters record RAL transitions and copies, restore source state, never own the source, and retire map→staging→completed submission. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_memory_recovery_contract` / `ral_memory_recovery_source_policy_contract` | always | Wired RAL portability | native-free OOM/fragmentation/pressure/resize/device-loss events publish one exact generation-bound bounded action: drain/retry, evict/retry, reduce quality, defer, recreate backend or fail. Retry ceilings and live-parent preservation are self-validating; Vulkan publishes queryable allocation-failure telemetry and WebGPU unknown-budget managed device loss uses the same policy. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_abi_contract` / `ral_webgpu_shader_abi_contract` / `ral_shader_abi_source_policy_contract` | always | Wired RAL portability | versioned native-free shader ABI records canonical stage modules, artifact provenance for SPIR-V/MSL/WGSL, bounded bind-group and vertex interfaces, specialization values, and Vulkan-push/WebGPU-uniform inline data. Every manifest, variant and fixed pipeline-state input enters a deterministic semantic key; unordered/duplicate/over-capacity/interface drift fails output-atomically. Vulkan pipeline creation may opt into exact pre-native validation while legacy callers remain compatible. Full corpus generation/translation is a later TASK-202.4 slice. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_artifact_catalog_contract` / `ral_shader_artifact_catalog_source_policy_contract` | Node 18+ | Wired shader toolchain | the canonical renderervk manifest emits one bounded, ordered native-free provenance row per SPIR-V symbol. Each row binds stage, expanded source, ordered defines, fixed `glslang -V` semantics, byte count and a digest byte-compatible with `Ral_ShaderArtifactDigest`; shader source/define/stage/artifact/order drift invalidates the committed freshness set before backend translation. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_translation_driver_contract` / `ral_shader_translation_source_policy_contract` | Node 18+; pinned SPIRV-Cross + Wired Naga for the real tool | Wired shader toolchain | a manifest-driven offline driver exact-joins the generated provenance catalog, resolved portable manifest and `shader_data.c`, invokes one version-checked translator per selected symbol, and publishes the immutable 292-module MSL/WGSL corpus plus artifact and manifest digests. Required-WGSL mode and canonical freshness checks fail closed on missing/extra/reordered/tool-version/byte drift; no runtime compilation exists. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_reflection_abi_contract` / `ral_shader_reflection_catalog_contract` / `ral_shader_reflection_source_policy_contract` | Node 18+; SPIRV-Cross for catalog regeneration | Wired shader toolchain | SPIRV-Cross JSON reflection is normalized into schema-v1 native-free entry, descriptor, vertex-input, inline-data and specialization rows, then exact-joined to all 292 canonical SPIR-V provenance entries. Directly portable compute layouts stay resolved; vertex-buffer formats, WebGPU inline-uniform locations, combined samplers, runtime arrays, sampler kind, unsized buffers and ambiguous storage access remain explicit lowering requirements rather than invented WebGPU ABI. Duplicate/out-of-range/stale rows fail closed. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_manifest_compose_contract` / `ral_shader_manifest_schema_contract` / `ral_shader_manifest_compose_source_policy_contract` | Node 18+ | Wired shader toolchain | a versioned native-free override catalog is exact-bound to SPIR-V digests and resolves only explicit reflection gaps before composing one compute or exact vertex+fragment schema-v1 RAL manifest. Binding stage unions, vertex formats, sampler kinds, bounded arrays, combined-sampler splits, buffer minimums, storage semantics and Vulkan-push/WebGPU-uniform fallback locations are deterministic; stale, unordered, conflicting, unknown or unresolved lowerings fail closed. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_shader_portable_manifest_contract` / `ral_webgpu_shader_catalog_contract` / `ral_shader_portable_catalog_source_policy_contract` | Node 18+ | Wired RAL WebGPU shader seam | all 292 canonical modules resolve to native-free portable reflection with 444 exact vertex formats, 241 bounded descriptor arrays, 91 split combined samplers and 15 deterministic inline-uniform bindings. The WebGPU-shaped consumer exact-checks the pinned manifest/toolchain/WGSL bytes and exposes only WGSL plus portable RAL ABI; stale bytes, unknown symbols, unresolved bindings and non-WebGPU `immediate` storage reject. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_metal_core_contract` / `ral_metal_bind_group_contract` / `ral_metal_render_contract` / `ral_metal_pipeline_contract` / `ral_metal_draw_contract` / `ral_metal_present_contract` / `wired_metal_window_smoke` / `ral_frame_shell_contract` / `ral_presentation_host_contract` / `ral_sdl_presentation_host_contract` / `ral_metal_module_contract` / Metal source/link policies / `ral_metal_shader_catalog_contract` | Apple + Node 18+ | Wired native Metal RAL | an Objective-C++ backend owns a real `MTLDevice`/`MTLCommandQueue`, resources and completed blit receipts; bounded portable bindings lower to argument buffers and portable attachment facts lower to native encoders. The canonical overlay MSL pair compiles offline to digest-bound metallibs and maps its portable ABI/state into a generation-bound PSO without runtime compilation. A real indexed draw exact-joins PSO, argument group, buffers and RGBA8+D32 targets, then validates all stored 8×8 pixels. A Metal presentation owner adopts the engine-owned main `CAMetalLayer`, selects SDR BGRA8/sRGB or FP16 extended-linear Display-P3, enforces one monotonic drawable transaction, performs a real clear/present completion and rebuilds size/mode through explicit generations. A versioned native-free presentation-host cohort lets the SDL3 main-window owner publish logical/pixel extent, scale, visibility and an opaque borrowed surface without leaking SDL or Metal types; the client-shaped host proves fallback, resize, keep/reload and full unload. A native-free frame shell spans Vulkan, Metal, GL43, WebGPU and WebGL2 lifecycle semantics, while the loadable `wired_metal_arm64` module exports renderer ABI 21, adopts/reconfigures that main surface, presents empty frames and tears presentation children down before the Metal core. Combined/unbounded/ambiguous layouts, artifact/layout/state/resource/range/presentation drift, duplicate/missing lifecycle operations, wrong native classes, loss and stale generations fail output-atomically. The module link policy requires Metal/QuartzCore/CoreGraphics while rejecting SDL3, MoltenVK and Vulkan; the standalone platform host links SDL3 separately. World/model/UI migration, arena content, monitor HDR and performance remain later slices. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `backend.metal` |
| `ral_legacy_material_contract` / `ral_legacy_material_source_policy_contract` / `vk_generic_specialization_contract` | always | Wired RAL portability | a native-free semantic legacy-Q3 material receipt names texture domains/combine modes, lightmap, alpha/fog/fixed/depth/entity state and source-stage environment/tcmod/deform facts. A table-driven opaque/lightmap/additive/alpha/fixed/fog/ATEST corpus distinguishes exact direct lowering from deterministic graceful fallback; malformed and over-capacity variants fail output-atomically. The Vulkan generic specialization bridge preserves the fixed 16-word map while consuming this shared decision, leaving WebGPU free of Vulkan types and word offsets. | contract | `host-only`, `subsystem`, `owner.ral`, `owner.renderer`, `failure.contract`, `feature.always` |
| `ral_buffer_map_state_contract` / `ral_vulkan_buffer_map_contract` / `ral_webgpu_buffer_map_contract` / `ral_buffer_map_source_policy_contract` | always | Wired RAL portability | native-free READ/WRITE buffer-map requests and generation-bound PENDING/READY tickets share one mutation-tested lifecycle: Vulkan publishes immediate READY with noncoherent invalidate/flush, a WebGPU-shaped fixture covers async completion/cancel/loss, production writers use device-local upload/staging and readbacks use COPY_DEST→HOST_READ with bounded typed MAP_READ, and mapped buffers are rejected by every buffer-consuming GPU command family until exact unmap/cancel. The legacy persistent-map API remains only as an allowlisted compatibility definition with zero product callers. | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_residency_policy_contract` | always | Wired RAL | backend-neutral page identity/state transition, strict visibility tier, integer screen-error×capped-staleness score, starvation/fairness, hard page/byte budget and fallback-safe eviction reference trace | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_temporal_contract` | always | Wired RAL + Vulkan frontend projection/history seam | backend-neutral eight-phase jitter, transactional history identity and invalidation; output-atomic projection math; canonical 2×FP16-color + 2×R32F-depth RAL resource create/reuse/extent-topology replace/partial-failure rollback/release | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_temporal_entity_cache_contract` | always | Wired renderer temporal identity | submit-transactional camera plus actually-visible entity pose receipts, generation/frame/model-topology compatibility, duplicate poisoning, cancel preservation and output-atomic invalid-input rejection | contract | `host-only`, `subsystem`, `owner.renderer`, `failure.contract`, `feature.always` |
| `ral_temporal_entity_source_policy_contract` | always | Wired cgame/Vulkan renderer | center-eye full-target primary-scene marking, portal/worldless/stereo rejection, hyperspace camera cut, pre-draw receipt scan and graphics-submit-bound commit authority | contract | `host-only`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `ref_entity_motion_contract` | always | Wired renderer/cgame ABI | fixed 24-byte pointer-free temporal identity POD, generation-keyed optional trap discovery, atomic two-pointer marshal, absence fallback and fail-closed invalid metadata | contract | `host-only`, `subsystem`, `owner.renderer`, `failure.contract`, `feature.always` |
| `ref_entity_motion_source_policy_contract` | always | Wired renderer/cgame ABI | REF v19 export wiring, capability-gated discovery, exact VM pointee bounds, stale-export fallback, and all renderer legacy-clear/accepted-stamp source seams | contract | `host-only`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `cg_temporal_identity_contract` | always | q3now cgame | stable observed-snapshot entity generation, append-only part roles, exact world-owner identity, overflow fail-ordinary and output-atomic producer metadata | contract | `host-only`, `subsystem`, `owner.q3now-cgame`, `failure.contract`, `feature.always` |
| `cg_temporal_identity_source_policy_contract` | always | q3now cgame | initial/transition/gap/restart lifecycle cuts, canonical predicted-player owner, eligible opaque base producers and ordinary effect/overlay exclusions | contract | `host-only`, `infrastructure`, `owner.q3now-cgame`, `failure.contract`, `feature.always` |
| `nav_coord_contract` | `USE_RECAST_NAVMESH=ON` | Wired nav | exact Quake/Recast axis, handedness and round-trip conversion | contract | `host-only`, `subsystem`, `owner.wired-nav`, `failure.contract`, `feature.recast` |
| `wired_scene` | `USE_LUA=ON` | Wired scene | Lua scene-table loader | contract | `host-only`, `subsystem`, `owner.wired-scene`, `failure.contract`, `feature.lua` |

Every host test has a 30-second timeout. A test target must compile the owned production algorithm/source rather than a test-only reimplementation; production/runtime targets must never link a test framework. `wired_curve` now compiles the owned source with the production `q_shared.h` configuration and explicitly does not enable its standalone fallback. This inventory still has seam debt: `wired_scene_eval` and `wired_scene` use their source files' standalone fallbacks, while `mdl_anim` exercises a header through a separate test translation unit. Those remaining modes are contracts for the owned logic, not proof that the shipped engine translation unit was linked and executed unchanged.

## Commands

```sh
make test-host
ctest --test-dir build/release --output-on-failure --no-tests=error -L host-only
ctest --test-dir build/release --output-on-failure -L owner.wired-scene
ctest --test-dir build/release --show-only=json-v1
```

`BUILD_TESTING=OFF` is the production-only configure boundary: host test targets are not created and no test dependency may be required.

## Visual renderer gates

`tests/visual-render-features.sh` owns computed pixel/log oracles rather than human-only screenshot inspection. In addition to GTAO, Forward+, viewport, alpha-tested shadow, scene and chromatic modes, the dynamic-light cohort now has two distinct contracts:

- `--mode dlight-shadow` captures shadow-on A/A, shadow-off B and no-light frames inside one engine/map/camera/resource generation. It requires bounded same-config noise, a visibly lit receiver, localized cast-shadow darkening, and rejects whole-frame dimming.
- `--mode dlight-shadow-lifecycle` runs K=4/four synthetic lights, requires the exact `4 lights x 6 = 24 passes/frame` producer row, a device-bounded atlas, a live on-to-off resource rebuild, post-toggle screenshots, zero unexpected VUID/crash and controlled shutdown. It is lifecycle evidence, not a multi-light style or GPU-time claim.
- `--mode entity-occlusion` asks whether a world-space entity survives the world draw. It captures the same fixed camera three ways — `r_drawWorld 0`, plain, and `r_drawEntities 0` — learns which tiles the entity occupies from the world-off frame instead of hardcoding them, and requires those specific tiles to still move when the world is drawn. The negative control is the point: "the world-on frame is not empty" is satisfied by the world alone, so the gate compares entity-present against entity-suppressed with the world drawn in both. Defaults target the TASK-177 repro (`EO_MAP=arenam3`, `EO_POS`, `g_gametype 6`), all overridable by environment. It detects that a visible entity stopped being visible; it does not attribute the cause.

`tests/dlight-shadow-gpu-budget-check.sh` is the companion cost authority. Five fresh fixed-camera processes measure the no-op interval and K=1..4 through a RAL-owned timestamp query pool. Every active case binds its atomic `samples=200 / active / K / 6K passes / gpu-ms` row to the exact CPU producer, while the self-test rejects missing pools/samples, wrong K/pass metadata, off-path work, invalid ordering, non-scaling timestamps, validation errors and provenance/result corruption. The sweep reports real GPU milliseconds for the later default-K decision; it does not make that product/readability choice itself.

The former separate-process dlight comparison is intentionally retired because cold renderer/resource drift could counterfeit the A/B signal. `--mode selftest` contains the assertion-level no-shadow, global-dim and unstable-noise rejection teeth.

## Sanitizer lane

`make test-sanitize-host` configures a separate `build/sanitize-host` tree and
builds only the `host_tests` aggregate. Every host contract is compiled and
linked with fail-fast AddressSanitizer + UndefinedBehaviorSanitizer; production,
game-VM and WASM targets are not instrumented. Leak detection is disabled so
the lane remains portable across supported Clang/GCC hosts; bounds, lifetime,
alignment, integer and undefined-behavior failures remain fatal. CTest writes
the durable machine-readable result to
`build/sanitize-host/host-sanitizer-results.xml`.

The sanitizer-only `sanitizer_runtime_contract` intentionally executes one
heap-use-after-free and one signed-overflow probe in child processes. It passes
only when both children exit nonzero with the expected ASan/UBSan diagnostics;
this prevents an ignored compiler flag or missing runtime from turning the lane
into a vacuous green.

```sh
make test-sanitize-host
ctest --test-dir build/sanitize-host --output-on-failure --no-tests=error -L sanitizer.asan-ubsan
```

## WiredUI process gates

These gates boot an assembled GUI product with an isolated home. They require a
current `pax21.sw3z`; `WIRED_CONTENT_ROOT` supplies the read-only canonical
`pax01.sw3z` (or legacy `pak0.pk3`) when it is not installed beside the binary.

| Command | Product contract |
|---|---|
| `make test-wiredui-menu-functional WIRED=...` | menu inventory primitives plus main → Host → selected map → first gameplay frame |
| `bash tests/wiredui-layout-checks.sh ...` | ordered main-menu keyboard/hover focus, DPI and single-highlight layout |
| `bash tests/wiredui-errordialog-check.sh ...` | automated/interactive error popup lifecycle across two map loads |
| `make test-wiredui-external-actions WIRED=...` | paired mod feeder selection, Load Mod, full game-directory restart, mounted mod marker and logging continuity |
| `make test-wiredui-bot-actions WIRED=...` | real in-game quick-add of two bots, bot-only feeder selection, verified bot kick and human/survivor protection |
| `make test-bot-slot-restart WIRED_BINARY_HEADLESS=...` | one headless process allocates the same named bot in the same slot across a full compound `stopserver` → `map` lifecycle; the old identity is refused and only the current identity removes the replacement, with retained binary/content/harness provenance |
| `make test-wasm-map-restart WIRED_BINARY_HEADLESS=...` | explicit `vm_game=1` stages an invalid loose AOT sentinel, proves interpreter-only startup plus two `map_restart` recreations without any AOT probe/server shutdown, reconnects the same allocation-1 bot, then proves stale identity rejection after a same-slot/name allocation-2 replacement |
| `make test-vmi-bytecode-gui WIRED=...` | paired isolated headless/GUI session with `vm_game=1` and `vm_cgame=1`: invalid loose AOT sentinels remain unopened, both production VMs load `.wasm` through the interpreter policy, and the client reaches arena7's first gameplay frame |
| `make test-reload-wasm-refusal WIRED=...` | `reload_wasm` is mutation-free: no live VM reports a no-op, while active headless `gamesv` and active local GUI `gamesv+gamecl` are refused as complete identity sets and continue until authored clean shutdown |
| `make test-vmi-pack-runtime WIRED=...` | fresh paired headless/GUI homes stage only shipped `pax01.sw3z` + `pax21.sw3z`; `sv_pure=1`, mode-2 gamesv/gamecl interpreter loads, admission/FIRST, exact SW3Z VM inventory/member hashes, and engine-owned `sysinfo` archive-member paths prove both VMs came from the staged pax21 rather than loose files |
| `make test-wiredui-demo-play WIRED=...` | empty Demo inventory refusal plus ESC/Back return-focus, same-process hot arrival/reload with a dotted basename, then the main-menu keyboard route to current-protocol demos, typed feeder selection, exact playback, advancing frames and natural EOF |
| `make test-wiredui-demo-malformed WIRED=...` | ten structural/semantic current-protocol cases, including a valid non-empty illegal top-level command and a valid `svc_snapshot` body with declared areabytes 255, through the real Demos selection/Play path, exact demo-local recovery, retryless popup over a reloaded Demos menu, authored dismissal, plus bounded structural attract continuation/non-recursion |
| `make test-wiredui-demo-io-fault WIRED=...` | one automated next-playback file-adapter fault after six real bytes, classified by the production frame reader as typed `io-error` through the authored Demos Play route, with exact retryless recovery and no structural/semantic/generic teardown substitution |
| `make test-wiredui-demo-disappeared WIRED=...` | a watcher removes one selected loose current-protocol demo between authored inventory selection and Play; the typed UI-owned open failure preserves connection state, rebuilds Main → Demos to an empty inventory, and presents a generic retryless popup without changing direct `demo`/`nextdemo` continuation policy |
| `make test-wiredui-demo-semantic-continuation WIRED=...` | exact `svc_snapshot/areabytes=255` demo-local recovery under two disjoint owners: attract advances frame-bounded to its successor panel, while scripted `nextdemo` executes exactly once without popup recovery or retained error state |
| `make test-wiredui-connect-action WIRED=...` | real Specify Server editfield input, invalid-address rejection, validated loopback connect and first gameplay frame |
| `make test-wiredui-server-browser WIRED=...` | two-row browser/status lifecycle, isolated Server Info Connect disposal with byte-identical late no-store, then direct unprotected popup Connect to real headless QUIC gameplay |
| `make test-wiredui-password-cancel WIRED=...` | protected prompt real pointer Cancel, menu ESC and edit-mode single ESC each erase transient secret state while preserving the same READY Server Info selection/status owner for natural reopen |
| `make test-wiredui-ingame-serverinfo WIRED=...` | in-game Server Info binds its request to the exact active QUIC connection/address rather than stale browser selection, hides browser-only actions, then Close restores in-game focus/pause and Resume preserves the match |
| `make test-local-listen-timeout WIRED=...` | a console-started integrated host (`map arena7`) is paused through real in-game ESC/Resume for 2500ms with `cl_timeout=1`; exact in-memory handles/slot, pause state, post-resume chat roundtrip and advancing server time prove the same live match without timeout/reconnect |
| `make test-ral-pipeline-runtime WIRED=...` | the real `ral_pipeline_test` command creates a fresh offscreen Vulkan/RAL backend and proves 100-way layout sharing/teardown, graphics draw/readback, exact compute SSBO output, a binary-semaphore exact-range residency acquire, pipeline-cache roundtrip and zero live diagnostic resources; the live renderer then performs two same-window typed swapchain-generation handoffs and, after each, binds a canonical acquired image, prepares it for presentation, issues graphics submit and completes present before post-sequence cgame dispatch and clean shutdown |
| `make test-ral-residency-runtime WIRED=...` | real arena7 adapter first loads the committed PBR base+packed-ORM resources, holds both `baseMip=1` parent views, tracks independent plane tickets/masks (`1→5`) and publishes both child views with one validated batch update only after the coherence group is complete; it then persists an exact per-mip page address/state record on one currently sampled single-plane texture, proves `resident→stale→in-flight→resident`, forces two policy-ranked whole-texture evictions and restores the same identities with white fallback, memory recovery and clean shutdown; analyzer self-test rejects 42 mutations |
| `make test-ral-frame-graph-runtime WIRED=...` | the opt-in renderer-owned Vulkan diagnostic derives three same-shape disjoint-lifetime resources from live native memory requirements, materializes one aliased physical allocation through the above-RAL frame graph, records/submits six transition-only passes through public RAL, waits the exact timeline, retires child textures before their allocation, and publishes a positive-saving receipt; retained arena1+arena17 validation runs reject VUID/error/fatal evidence and the analyzer self-test rejects 33 mutations |
| `make test-ral-profile-markers WIRED=...` | default-off semantic marker lane arms one arena7 frame (or allowlisted `WIRED_MAP=arena1`) with optional effects pinned out, requires Vulkan debug-utils to emit and balance exactly the `wired.main`, `wired.tonemap`, `wired.ui`, `wired.scene-depth-resume`, and `wired.present` dynamic-rendering label scopes (`requested=begin=end=5`, mask `0x201d`), rejects every VUID, then cleanly shuts down; analyzer self-test rejects 21 mutations |
| `make test-ral-profile-renderdoc WIRED=...` / `--self-test` | the *independent-tool* half of the marker claim: the sibling gate above proves the engine reports five balanced scopes, this one requires a real GPU tool to resolve them. A capture readout must expose the five semantic names exactly once each, in recorded order, each with nested children (a resolved marker, not a flat action name), with strictly ascending event ids, a nonzero action count inside the scopes, no unbalanced scope, and `labelSource=vulkan-debug-utils`. Live capture needs a RenderDoc host (Windows/Linux — RenderDoc ships no macOS build) and SKIPs elsewhere; the analyzer runs everywhere and rejects 24 mutations. Registered in ctest as `ral_profile_renderdoc_analyzer_contract`. Optick is explicitly *not* the consumer here — it cannot read debug-utils labels at all (see `wiki abandoned-directions`, 2026-08-20) |
| `make test-ral-profile-layout WIRED=...` | `r_gpuSpeeds=1` accumulates ordered semantic timestamp lanes instead of positional slots; a bounded 120-sample RAL history exposes oldest→newest graph data plus latest/average/min/max snapshot through `ral_dump live profile`; one fresh arena7 process toggles SMAA `0→1→0` and requires exact topology epochs/snapshots `baseline → baseline+smaa → baseline`, discarding incompatible samples at each transition before clean shutdown; analyzer self-test rejects 31 mutations |
| `tests/ral-temporal-projection-check.sh --self-test` / `tests/ral-temporal-projection-check.sh <wired>` | default-off Phase 7.10 arena7 gate under a provenance-pinned fixed-1ms-per-render-tick stationary third-person camera and deterministic synthetic one-joint/two-frame IQM animation. The production loader maps all 36 animation roles and uploads the fixture VBO; adjacent armed cohorts exact-join one opaque GPU-IQM WRITE segment, online tagged generic/IQM order, payload/factory generations and current→previous palette continuity with accepted submit and matching command-slot fences. The H3c witness copies seven centered 256-core-plus-apron planes before bloom and requires canonical validity, nonzero motion, accepted reprojection influence, precise texel-space arithmetic, exact available fallback classes, populated hashes, immutable device-depth decoding and one recursive feedback link. The analyzer rejects 138 hostile mutations; independent retained N=2 roots require matching fixture/tool/binary provenance but not matching GPU output hashes. Disable repeats safe child-first teardown and restores timing controls. Per-IQM-pixel attribution, runtime INVALIDATE output, reference-correct vectors/visual quality, broad skeleton/material/multi-surface coverage, ATEST/transparency/CPU-IQM, default timing, validation-backed VUID cleanliness, full-frame proof, screenmap/portal/stereo/split/WASM and future normal-map IQM semantics remain explicit nonclaims. |
| `make test-ral-profile-host WIRED_PROFILE_HOST=...` | macOS/MoltenVK standalone SDL3 + RAL tool window: an RGBA32 font atlas and fixture-backed profile UI (including a separately registered RAL texture) produce non-empty indexed ImGui draws with exact font→external→font binding, canonical prepare/checked-submit/present across physical-pixel resize and restore, while minimize suspends acquire/submit/present/ImGui work; non-Darwin runtime skips, and validation-backed VUID proof, pixel readback, and native out-of-date/surface-loss recovery remain explicit nonclaims |
| `make test-ral-profile-live WIRED_PROFILE_HOST=... WIRED=...` | macOS/MoltenVK product→separate-tool live path: the renderer exposes a pointer-free completed semantic timestamp sample, the client sends default-off token-bound latest-value UDP to strict loopback, and the standalone RAL/ImGui host presents changing live history; a real `vid_restart` yields terminal→waiting→new producer session/history before final terminal and clean shutdown, with fresh N=2 retained evidence and fail-closed analyzer self-mutations |
| `ral_profile_imgui_contract` | `BUILD_TESTING=ON` | Wired RAL/tooling | production profile snapshot + frame-major history render into the vendored Dear ImGui table/plots, real SDL3 keyboard/text/mouse/wheel events feed ImGui IO, non-empty draw data is produced, and invalid topology/history/input is rejected output-atomically; adapter is not linked to shipping GUI/headless targets | contract | `host-only`, `subsystem`, `owner.ral`, `owner.tools`, `failure.contract`, `feature.imgui` |
| `wired_profile_imgui_ral_contract` | `BUILD_TESTING=ON` | Wired RAL/tooling | the production ImDrawData backend uploads the real RGBA32 atlas, grows/reuses host-coherent vertex/index buffers, honors framebuffer-scaled clip rectangles plus global `IdxOffset`/`VtxOffset`, switches registered texture bind groups, restores render state, and rejects stale/unknown textures, invalid callbacks and out-of-bounds geometry before recording | contract | `host-only`, `subsystem`, `owner.ral`, `owner.tools`, `failure.contract`, `feature.imgui` |
| `r_profile_telemetry_codec_contract` | `BUILD_TESTING=ON` | Renderer/client/tooling | pointer-free little-endian latest-sample packets are exact-length and CRC-bound, copy semantic labels, reject non-finite/duplicate/corrupt input output-atomically, and remain below the bounded UDP packet ceiling | contract | `host-only`, `subsystem`, `owner.renderer`, `owner.tools`, `failure.contract` |
| `wired_profile_telemetry_udp_contract` | `BUILD_TESTING=ON`, loopback | Wired tooling | the token-bound nonblocking receiver accepts monotonic value-owned semantic samples, derives bounded local history, rejects duplicate/out-of-order identity, resets on producer session/topology change, marks stale data inactive and consumes an explicit renderer-stopped terminal without tearing down the tool | contract | `host-only`, `subsystem`, `owner.tools`, `failure.contract` |
| `ral_swapchain_policy_contract` | `BUILD_TESTING=ON` | Wired RAL | backend-owned surface selection clamps extent/count, honors ordered format/mode preferences and exact usage, materializes more than eight canonical renderable images, validates acquire/present output-atomically, and proves recreate failure retires/destroys the typed old generation with no rollback | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_mrt_contract` / `ral_mrt_source_policy_contract` | `BUILD_TESTING=ON` | Wired RAL | legacy zero blend masks remain write-all while explicit zero is exact; without enabled independent blend, distinct effective attachment states reject before pipeline-layout/shader/Vulkan pipeline creation; independent blend is enabled/capped only with owned-device request+support and imported devices fail closed; optimal-tiling format queries include every implicit sampled/upload/readback usage bit used at image creation | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_bindless_publication_contract` / `vk_bindless_publication_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer bindless publication ledger | fixed 4096-image/32-sampler ledgers publish monotonic set/pool/transaction/slot epochs, atomically author multi-view cohorts, tombstone releases, poison all stale receipts after a real descriptor write whose publication fails, and expose an ordinary receipt only for an exact legacy combined-descriptor/view/sampler owner cohort; binding-2 2D-array publication remains a separate allowlisted writer | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `vk_temporal_iqm_payload_contract` / `vk_temporal_iqm_payload_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer temporal IQM payload owner | exact-enabled product owner materializes one fixed 3,194,880-byte device-local STORAGE+TRANSFER_DST payload per visited command slot, authors generation-bound records once in an owner CPU shadow, seals with one exact queue upload, revalidates shadow bytes, and exposes exact set0/binding0 vertex-stage receipts; current-slot fence reuse, candidate-first idle replacement, monotonic owner/slot/prepare epochs, layout leases and child-first disable/shutdown remain fail-closed, while default OFF performs no payload work | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `vk_temporal_iqm_exact3_factory_contract` / `vk_temporal_iqm_exact3_source_policy_contract` / `vk_temporal_iqm_exact3_shader_reflection_contract` | `BUILD_TESTING=ON`, glslang/SPIR-V tools for reflection | Vulkan renderer temporal IQM exact-three path | the active product composite layout and atomic WRITE/INVALIDATE pipeline pair borrow payload set 0, key the exact bindless set-1 cohort, expose an 8-byte fragment push ABI, and pin direct-indexed current/previous position skinning plus RGBA16F/RG16F/R8 outputs without whole-record shader copies. Primary pre-scan, payload authoring and the command adapter bind exact pipeline/set0/set1/push/V-I operands, draw with `firstInstance=recordIndex`, share first-CLEAR/later-LOAD with generic temporal segments, resume MAIN with LOAD and publish only after exact Commit/Finish/submit; normal/tangent shading, ATEST/transparency, CPU-IQM and broad runtime coverage remain nonclaims | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_ral_attachment_translate_contract` / `vk_ral_attachment_translate_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL adapter | exact heterogeneous scene/RG16F/R8 attachment order and per-target blend/write masks survive Vulkan→RAL translation, count-zero is unambiguous depth-only, invalid/mismatched/undefined input rejects output-atomically, scalar special-pipeline compatibility remains single-attachment-only, and the production owned RAL backend requests independent blend before creation | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `tr_temporal_motion_contract` / `tr_temporal_motion_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer temporal prerequisite | pure per-draw policy distinguishes valid static world/MOD_BRUSH motion, coverage-authoritative opaque invalidation, overlay preservation and deferred ATEST; unjittered committed camera/entity receipts produce output-atomic current/previous Vulkan clip, top-left UV and signed velocity with exact Q3 matrix order, model compatibility and perspective-w rejection; no runtime pass/pipeline/shader integration is claimed | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `tr_temporal_motion_targets_contract` / `vk_temporal_pipeline_cohort_contract` / `tr_temporal_motion_structural_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer inert temporal-MRT prerequisite A | transactional backend-neutral RAL-owned RG16F velocity + R8 validity targets preflight independent blend and exact color/sampled/transfer-src format usage, retain the live generation on partial replacement failure, and tear down all views before textures; an unreachable exact-three-attachment PRESERVE sibling contract keeps the scene blend and explicitly masks auxiliary writes. A2b may materialize only the inert targets; preserve/write/invalidate pipeline slots remain unbuilt and unselected, with no temporal shader module, runtime pass/bind, unconditional draw-uniform growth, or GPU readback. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `tr_temporal_motion_payload_contract` / `tr_temporal_motion_payload_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer active-only temporal draw payload prerequisite | a RAL owner whose mutation seam is production-callable only through A2a materializes a two-storage-buffer composite set-3 layout and per-command-frame host-coherent 144-byte-record array keyed by the borrowed entity-buffer pointer plus allocation generation; A2b may read only its generation-safe layout publication. Candidate-first growth/rebind requires fence authority, rejects cross-frame aliases, preserves live state on every failure, writes strict monotonic absolute entity slots, and detaches the composite group before borrowed-buffer destruction. No shader/pass/bind activation, extra ordinary descriptor set, or change to the 608-byte ordinary draw UBO exists. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_entmat_adoption_contract` / `vk_temporal_entmat_adoption_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer raw-entMat adoption prerequisite | a four-slot owner callable only by the A2a runtime seam candidate-adopts non-owning wrappers over exact native-buffer/size/nonzero-generation identities after completed-fence authority, publishes a consumer rebind before retiring the prior wrapper, rejects cross-slot aliases and exhausted generations, and records partial-release detaches so idle-proven retry never re-detaches a consumer. It has no pass/bind/draw/pipeline/module authority and does not change the 608-byte ordinary draw UBO. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `tr_temporal_batch_request_contract` / `vk_temporal_entmat_runtime_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL A2a | a pointer-free command-batch receipt provides exact/conflict/consume-once authority to the matching draw-buffer token; a fresh/default-OFF session performs zero incremental owner/RAL calls, and only an enabled exact receipt conjoined with latched `r_entitySSBO` materializes the generation-safe adopted entMat wrapper plus payload owner after the slot fence. A later disabled exact receipt performs one idle-proven teardown before raw-parent destruction. A2a itself has no payload recording, pipeline-factory, pass, bind or draw authority; A2b separately owns the inert target/layout continuation below. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_motion_materialization_contract` / `vk_temporal_motion_materialization_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL A2b | after an enabled exact A2a receipt succeeds and its extent exactly matches the current physical render target, an inert aggregate owner transactionally materializes RG16F velocity plus R8 validity targets and one build-shaped generic raw/adopted pipeline layout over ordinary sets 0-2 plus the generation-safe composite payload set 3. Same-resource semantic receipt refresh is zero-churn; allocation generations survive disable/re-enable; replacement requires global idle; aggregate failures clear ready and retain partial owners only for retry/cleanup. Disable and full shutdown release layout, targets, payload/adoption and deferred RAL work before borrowed BGL/raw-parent teardown. No pipeline factory, shader module, payload append/get-group, pass, bind or draw authority is active. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_resolved_hdr_contract` / `vk_temporal_resolved_hdr_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL H2a/H2b | an active-only, post-fence owner materializes one exact full-target RGBA16F storage/sampled/color/transfer resolved-HDR texture plus complete postprocess and histogram sampling bind groups, keyed to a monotonic raw scene-color attachment generation and every borrowed parent identity. The command-local producer is either an exact full-extent current→H2 COPY bootstrap/fallback or the H3 resolve; the same routed H2 cohort then feeds Store before bloom, bloom/composite, histogram and tonemap. Its content receipt is prepromoted against the staged history candidate, committed by the backend, and only then published durably. Default OFF performs no resolved allocation, RAL copy, route publication, target bind, Store or submit log; disable, attachment rebuild and shutdown release+drain H3c/H3/H1/Store/H2 before borrowed layouts/buffers/raw scene parents. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_resolve_contract` / `vk_temporal_resolve_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL H3b | after exact history preflight and a sealed pending MAIN activation, an active-only compute owner samples current HDR/device depth, the exact prior committed RGBA16F/R32F feedback source, and generation-bound RG16F/R8 motion. Jitter-correct texel-space reprojection uses integer current fetches, manual bilinear prior sampling, finite/validity and absolute-or-relative depth rejection, and a 3x3 current-color clamp before writing H2. The full ticket binds the prior producer/source receipt plus activation, owner, H2 target and exact future RESOLVED_FEEDBACK Store write; producer prepromotion must succeed before backend history commit, after which the cached ticket is published without a second fallible validation. H3 failure retains the routed COPY fallback but stages no recursive history candidate. No full-frame/reference-quality or visual TAA claim is made. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_resolve_readback_contract` / `vk_temporal_resolve_readback_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL H3c | a default-off explicit two-capture arm allocates one TRANSFER_DST+MAP_READ staging buffer per command slot after that slot's fence, records seven centered 256-core-plus-apron texture/depth-plane copies after successful H3 route publication but before Store/bloom, transitions the completed copy to HOST_READ, and maps only within the exact slot-fence completion call. The exact promoted H3 ticket binds all 48 push bytes, prior feedback and current producer/product receipts, depth encoding, buffer generation and command slot; only the same slot's completed fence may run the CPU shader oracle. Six Vulkan depth encodings, texel-space bilinear reprojection, depth rejection, 3x3 clamp, 0.875 blend, explicit SPIR-V `NoContraction` round points mirrored by software binary16 RNE with at most one stored-half step, exact alpha/current/zero fallback, canonical R8 validity, populated-plane hashes and nonzero motion influence are pinned by hostile host mutations. Runtime N=2 additionally joins F1's committed Store receipt to F2's prior source and requires F1 resolved hash to equal F2 previous-color hash. OFF adds no H3c helper calls or product behavior, and child buffers release before every borrowed temporal parent. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_generic_recipe_table_contract` / `vk_temporal_generic_recipe_table_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer A2c1/A2c2a | an active-only heap side table deep-copies exact MAIN generic pipeline recipes only after A2b authority and ordinary pipeline publication, reverse-identifies the live ordinary shader pair against the generated 40-pair catalog, and publishes generation-safe pointer-free receipts. A synchronous output-atomic rehydration view rebuilds the exact VS1/FS15 Vulkan graph entirely from view-owned storage; it retains no original stack pointer. Rejection invalidates the slot, rewind/destroy evicts before ordinary pipeline retirement, and OFF keeps the owner unallocated. No pass, bind or draw authority is added. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `vk_temporal_generic_pipeline_table_contract` / `vk_temporal_generic_pipeline_table_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL A2c2b | at the exact post-fence/pre-acquire boundary, a complete generation-bound shader registry resolves each captured recipe's canonical ordinary VS/FS pair and an active-only direct-index table owns its unreachable PRESERVE/WRITE/INVALIDATE RAL candidates. MAIN pipelines cached before activation are rebuilt candidate-first: the old ordinary sibling remains published until the replacement recipe and all three temporal candidates succeed; any failure restores ordinary rendering and marks the slot declined for the current owner epoch. Exact3 retirement is batched through one same-backend deferred drain before layout lease release and before recipe/A2b/raw-parent teardown. Cold OFF allocates no table and performs no scan or factory call. No temporal pass, bind, draw, payload append or GPU-output authority is added. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_motion_recording_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL A2c3 | an exact command-batch/A2b/payload/table/physical-capacity authority begins the inert payload slot epoch before acquire and admits only one full-target primary MAIN command. Only admitted fresh-slot WRITE/INVALIDATE draws are exact3-preflighted and appended at the same absolute entMat slot before the infallible mapped entMat copy; PRESERVE/ATEST/non-admitted draws remain gaps, repeated physical siblings reuse the ordinary slot, and any admitted append failure poisons the pre-submit CPU diagnostic receipt. Temporal pass, bind, draw selection and GPU/runtime-output claims remain absent. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_main_activation_contract` / `vk_temporal_main_rendering_contract` / `vk_temporal_motion_readback_contract` / source-policy contracts | `BUILD_TESTING=ON` | Vulkan renderer/RAL A2c4a/A2c4b/A2c4c + H5a | exact generic and admitted opaque GPU-IQM draws append one shared command-adjacent tagged stream after successful logical emission. PRESERVE stays in ordinary MAIN; WRITE/INVALIDATE end ordinary rendering, barrier into generation-bound scene+RG16F+R8 LOAD/CLEAR, issue the logical draw once, then barrier and resume scene/depth/stencil with LOAD/STORE. Generic and IQM families share exactly one first auxiliary CLEAR; IQM additionally exact-joins immutable pre-scan facts, live geometry/bindless/payload/factory receipts and full command operands before Commit/Finish. Publication occurs only after successful graphics submit. The fenced diagnostic reports bounded shared RG16F/R8 content and generation-bound IQM payload/palette continuity; it does not prove per-pixel IQM attribution, runtime INVALIDATE output, reference-correct motion, ATEST/transparency or special/multiview paths. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `ral_temporal_contract` / `vk_temporal_history_consume_contract` / `vk_temporal_history_consume_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL H1 | generation-bound physical history receipts admit an explicitly armed, active-only compute witness of both the prior RGBA16F color and R32F linear depth before the current-slot store. Each slot writes a device-local STORAGE+TRANSFER_SRC witness, copies it into distinct TRANSFER_DST+MAP_READ staging, and performs bounded typed MAP_READ only after submit plus that slot's fence. Bootstrap never dispatches or consumes an arm; read/write slots and all borrowed identities are exact; submit and slot-fence authority gate publication. Default OFF/unarmed behavior adds no consumer allocation, dispatch, transition, copy or log. This is sampling provenance only: bloom/tonemap output and visual temporal resolve remain unchanged. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_history_store_contract` / `tr_temporal_submit_contract` / `vk_temporal_history_store_shader_reflection_contract` | `BUILD_TESTING=ON` | Vulkan renderer/RAL H4 | a post-fence candidate-first Store owner binds the exact H2 RGBA16F target, current device depth and two history color/depth write pairs, rejecting new/old borrowed and live-owned aliases before publishing its generation. Prepare performs all final-depth/MAIN visibility and immutable push preflight; producer then records COPY seed or H3 resolve, and Store texel-fetches that exact H2 source plus current depth before staging one output-atomic pending write. The pure submit decision cancels successful frames with no candidate, rejects omitted/wrong staged authority, prepromotes the typed producer, commits exact backend history all-or-fatally, and only then publishes H1/H2/H3/H3c receipts. Store and diagnostic children release before H2/history/depth parents with one idle-proven drain. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_shader_cohort_contract` / `vk_temporal_generic_catalog_contract` / `vk_temporal_shader_reflection_contract` / `vk_temporal_shader_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer inert opaque temporal shader/interface prerequisite | manifest-derived exact 40 compatible generic pairs select 36 temporal VS, 20 WRITE FS and 20 INVALIDATE FS with exact ordinary blob provenance; TX0 entity color aliases only the fixed-color VS, environment affects VS only, and shader UBO fog remains a distinct catalog key independent of the compile-time build push-fog layout. Pairwise reflection pins scene locations, named input/output types, storage classes, block layout, specialization and execution modes while source policy pins the shared scene-result write before temporal auxiliaries. Full descriptor-pointee equivalence, mathematical pixel equivalence and runtime rendering remain nonclaims. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_temporal_pipeline_factory_contract` / `vk_temporal_pipeline_factory_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer legacy temporal layout/factory prerequisite | candidate-first exact four-set raw temporal-main layout with non-owning RAL adoption, payload-layout generation and pipeline leases; representative PRESERVE/WRITE/INVALIDATE plus legacy IQM INVALIDATE recipes retain direct-SPIR-V/fingerprint/state and deferred-drain coverage. This legacy factory remains without production call authority; H5a product IQM uses the separate `vk_temporal_iqm_exact3_factory` owner and command path above. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `owner.ral`, `failure.contract`, `feature.always` |
| `vk_generic_specialization_contract` / `vk_generic_specialization_source_policy_contract` | `BUILD_TESTING=ON` | Vulkan renderer generic-pipeline specialization fixture | the production author and temporal factory share one exact caller-owned VS1/FS15 map schema; a product-shaped host fixture pins stage ownership, map order/offset/size, raw word preservation and fail-closed temporal admission for entity transforms, passive alpha/A2C/discard, texture-domain/fog ranges and finite fixed/depth-fade values. This fixture does not activate or materialize temporal pipelines. | contract | `host-only`, `subsystem`, `infrastructure`, `owner.renderer`, `failure.contract`, `feature.always` |
| `ral_submission_lifecycle_contract` | `BUILD_TESTING=ON` | Wired RAL | checked queue-submit result, strict command-buffer state, renderer-only external-lifecycle bridge, acquire/prepared image ownership and typed present transitions across all supported source layouts | contract | `host-only`, `subsystem`, `owner.ral`, `failure.contract`, `feature.always` |
| `wired_profile_host_lifecycle_contract` | `BUILD_TESTING=ON` | Wired tooling/RAL | deterministic resize coalescing, minimize suspension, restore, acquire/present result reduction, preflight-retain vs one-way failure and distinct surface-loss policy | contract | `host-only`, `subsystem`, `owner.ral`, `owner.tools`, `failure.contract`, `feature.imgui` |
| `imgui_tool_policy_contract` | `BUILD_TESTING=ON`, Ninja | Wired tooling/build | exact seven-source Dear ImGui adapter/backend cohort compiles only in `wired_profile_imgui`, never inherits `HEADLESS`, and has only the exact contract-test and tool-host consumers rather than shipping GUI/headless | contract | `host-only`, `infrastructure`, `owner.ral`, `owner.tools`, `failure.build-graph`, `feature.imgui` |
| `ral_profile_host_policy_contract` | `BUILD_TESTING=ON`, Ninja | Wired tooling/build | the SDL profile host compiles only under `WIRED_BUILD_IMGUI_TOOLS`, links standalone RAL+SDL+the isolated ImGui archive rather than renderer/product/headless libraries, and is absent from tools-off shipping graphs; the UDP sender is client-only, receiver is tool/test-only, and the shared codec has an exact client/tool/test owner allowlist with no renderer socket dependency | contract | `host-only`, `infrastructure`, `owner.ral`, `owner.tools`, `failure.build-graph`, `feature.imgui` |
| `make test-wiredui-global-browser WIRED=...` | authorized loopback master malformed-packet atomicity, deadline expiry/inactive replay, fresh-generation recovery, challenge-bound directed info hydration and real keyboard selection; Connect remains a separate regression |
| `make test-directed-ping-timeout WIRED=...` | direct `/ping` transaction logs: same-address fresh generation after timeout, byte-identical stale challenge rejection, and one current directed response acceptance; no cache/row claim |
| `make test-directed-ping-queue WIRED=...` | 34 direct `/ping` transactions: a >=500ms valid completed result is retained while a free slot exists and reclaimed before a live request under saturation; an all-live saturated queue then evicts oldest, its stale response is inactive, and the current replacement is accepted |
| `make test-ping-owner-browser WIRED=...` | direct and active global-browser ping transactions sharing one endpoint: exact owner/generation routing, current-consumer global-only cache publication, direct survival across authored refresh, and exact final selection identity |
| `make test-ping-owner-offscreen WIRED=...` | a held Global transaction completed after a real source switch to Local: the all-browser terminal reaper publishes it with `consumer=offscreen` only to Global, clears the exact identity, and exposes the row only after Global is reopened |
| `make test-ping-owner-expired-offscreen WIRED=...` | a held Global transaction expires while Local is active under a covering menu: the explicit Local consumer clears only the offscreen Global cache and exact identity; reopening shows no row until an authored fresh Global generation recovers and is selected |
| `make test-ping-owner-capacity WIRED=...` | an accepted Global result covered by another menu while 32 direct transactions saturate the shared queue: completed-slot reuse publishes through `consumer=capacity` before retirement, and the browser row appears after returning |
| `make test-lan-discovery-timeout WIRED=...` | concurrent manual ping and authored LAN discovery ownership, explicit deadline/inactive rejection, fresh-generation stale challenge rejection, one committed recovery row and keyboard selection |

Analyzer-only mutation checks are separate and do not constitute a product run:
`make test-wiredui-external-actions-self`, `make test-wiredui-bot-actions-self`, `make test-wiredui-connect-action-self`, `make test-wiredui-demo-play-self`
and `make test-bot-slot-restart-self`
and `make test-wasm-map-restart-self`
and `make test-vmi-bytecode-gui-self`
and `make test-reload-wasm-refusal-self`
and `make test-vmi-pack-runtime-self`
and `make test-wiredui-demo-disappeared-self`
and `make test-wiredui-server-browser-self`
and `make test-wiredui-password-cancel-self`
and `make test-wiredui-ingame-serverinfo-self`
and `make test-local-listen-timeout-self`
and `make test-ral-pipeline-runtime-self`, `make test-ral-residency-runtime-self`, `make test-ral-profile-markers-self`, `make test-ral-profile-layout-self`
and `make test-wiredui-global-browser-self`
and `make test-directed-ping-timeout-self`
and `make test-directed-ping-queue-self`
and `make test-ping-owner-browser-self`
and `make test-ping-owner-offscreen-self`
and `make test-ping-owner-expired-offscreen-self`
and `make test-ping-owner-capacity-self`
and `make test-lan-discovery-timeout-self`
and the corresponding scripts' `--self-test` modes.
